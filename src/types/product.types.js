import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const product.types = () => {
  return (
    <View>
      <Text>product.types</Text>
    </View>
  )
}

export default product.types

const styles = StyleSheet.create({})